[user]
	name = Arturs Mekss
	email = arturs.mekss@gmail.com

[alias]
  	c = checkout
  	po = push origin

[color]
  	diff = auto
  	status = auto
  	branch = auto

[core]
  	excludesfile = ~/.gitignore
  	editor = subl -n -w
  	autocrlf = input

[format]
  	pretty = %C(yellow)%h%Creset %s %C(red)(%an, %cr)%Creset

[github]
  	user = AMekss
[push]
	default = simple
[credential]
	helper = osxkeychain
[lfs]
	fetchexclude = images/
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[init]
	defaultBranch = main
[url "git@github.com:"]
	insteadOf = https://github.com/
